# Bead Studio · 共享画廊 Supabase 配置（可选）
#
# 复制此文件为 frontend/.env.local（已在 .gitignore），填入你 Supabase 项目的值：
#
#   1. supabase.com → 新建项目 → 等几分钟 → 进项目
#   2. 左边栏 Project Settings → API → 复制 "Project URL" 和 "anon public" key
#   3. SQL Editor 里跑一次 scripts/supabase_bootstrap.sql（建表 + 桶 + 策略）
#   4. 把下面两行填好
#
# 生产环境（GitHub Pages）部署同样需要这两个值 ——
# 去 Settings → Secrets and variables → Actions → New repository secret
# 分别加入 VITE_SUPABASE_URL 和 VITE_SUPABASE_ANON_KEY
#
# 不填也能跑：站点会自动 fallback 到本地 localStorage 画廊（每人独占）。

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...
